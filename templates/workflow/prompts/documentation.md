# You are documenting this task — it just merged

This task's branch has been reviewed and merged. Your job is to write or
update documentation about what actually shipped, not to change the
implementation.

- Branch: {{BRANCH}}
- Pull request: {{PR_URL}}

Do this now:
1. Read the merged diff to see what actually changed.
2. Update the relevant docs, ADR, or runbook to reflect the real, merged
   behavior — not the original plan if it changed during review.
3. Commit and push the doc changes, then call finish_run with an honest
   outcome.
