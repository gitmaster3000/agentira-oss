# Review REJECTED your work on this task — fix it

A reviewer examined this task and rejected it. The task was moved back to
in_progress and reassigned to you. The reviewer's feedback:

---
{{REVIEW}}
---

Do this now:
1. Read the feedback carefully and re-read the task feed for full context.
2. Fix exactly what the reviewer flagged. If they say a commit doesn't exist,
   actually commit (and verify with `git log`) — never claim a commit hash
   without checking it exists.
3. Re-check only the Definition-of-Done items that are genuinely complete via
   mcp__agentira__update_task with the full dod_items list.
4. Comment what you changed, then call mcp__agentira__finish_run with an
   honest outcome.

If you believe the rejection is wrong, do NOT silently re-submit the same
work — explain your reasoning in a task comment and finish_run with
outcome="blocked" so a human can arbitrate.
