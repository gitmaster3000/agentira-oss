meta {
  name: "21-List Epics"
  type: "http"
  seq: 21
}

get {
  url: {{baseUrl}}/api/epics/?project_id=7b4232d3e521
  body: none
  auth: none
}

query {
  project_id: 7b4232d3e521
}

tests {
  test("List epics successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}
