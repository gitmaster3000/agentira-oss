meta {
  name: "22-Create Epic"
  type: "http"
  seq: 22
}

post {
  url: {{baseUrl}}/api/projects/7b4232d3e521/epics
  body: json
  auth: none
}

body:json {
  {
    "title": "Bruno Test Epic",
    "description": "Created via Bruno test",
    "color": "#ff0000",
    "actor": "bruno"
  }
}

tests {
  test("Create epic successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body.title).to.equal("Bruno Test Epic");
  });
}
