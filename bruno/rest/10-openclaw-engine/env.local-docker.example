# Copy values into Bruno environment "local-docker" (bruno/environments is gitignored).
# Or:
#   bru run rest/10-openclaw-engine --env-var baseUrl=http://127.0.0.1:8000 --env-var apiKey=$AGENTIRA_API_KEY

baseUrl=http://127.0.0.1:8000
apiKey=<admin JWT or service token>
