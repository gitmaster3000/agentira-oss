meta {
  name: Deploy - Delete the throwaway project
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/api/projects/{{_deploy_pid}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{_deploy_token}}
}

assert {
  res.status: eq 200
}
