meta {
  name: List Epics Empty
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/epics/?project_id={{_rest_pid}}
  body: none
  auth: none
}

tests {
  test("List epics returns empty array initially", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}
