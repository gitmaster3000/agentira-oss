meta {
  name: Project Has Key Prefix
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/projects/{{_rest_pid}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.key_prefix: isDefined
  res.body.key_prefix: matches [A-Z]{2,10}
}
